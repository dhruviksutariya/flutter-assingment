import 'package:flutter/material.dart';

import 'Homescreen.dart';

void main() => runApp(Program78());

class Program78 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Program78',
      home: Homescreen1(),
    );
  }
}
