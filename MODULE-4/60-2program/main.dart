import 'package:flutter/material.dart';

import 'FoodScreen1.dart';

void main() => runApp(Program602());

class Program602 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: 'Material App', home: ToolbarDesign());
  }
}

